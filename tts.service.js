// Compatibility entry point for existing imports. The implementation lives in
// src/services so every page uses the same Web Speech instance and voice logic.
export { ttsService } from './src/services/tts.service.js';
